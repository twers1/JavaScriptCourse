// В этом задании в нашу функцию testStr передаются две строки. Вам нужно вернуть из функции их суммарную длину.

// Sample Input: 5794675 181683

//Sample Output: 13

// Решение: 
function testStr(a, b) {
    var c = (a+b)
    return c.length;
} 
